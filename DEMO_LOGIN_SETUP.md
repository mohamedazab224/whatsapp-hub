تم إضافة إمكانية **تسجيل الدخول بمستخدم تجريبي** إلى المشروع!

## 🎯 بيانات الدخول التجريبية

```
البريد الإلكتروني: demo@alazab.com
كلمة المرور: Demo@12345678
```

## 📋 خطوات الإعداد

### 1️⃣ إنشاء حساب Demo (اختر طريقة واحدة)

**الطريقة الأولى: من خلال Supabase Dashboard (يدويًا)**
1. اذهب إلى: https://app.supabase.com
2. اختر مشروعك
3. اذهب إلى: **Authentication > Users**
4. انقر على **"Add user"**
5. أدخل البيانات:
   - البريد الإلكتروني: `demo@alazab.com`
   - كلمة المرور: `Demo@12345678`
6. تأكد من تفعيل الخيار **"Auto confirm user"**
7. انقر **"Create user"**

**الطريقة الثانية: من خلال Script (تلقائي)**
```bash
npm run setup-demo
```

أو:
```bash
node scripts/setup-demo-user.js
```

### 2️⃣ استخدام Demo Login

على صفحة تسجيل الدخول (`/login`):
1. ستجد ثلاثة أزرار:
   - تسجيل الدخول البريدي (Email)
   - تسجيل الدخول عبر Google
   - **تسجيل دخول تجريبي** ← الجديد! 🎉

2. انقر على **"تسجيل دخول تجريبي"**
3. سيتم تسجيل الدخول تلقائياً بحساب Demo
4. ستنتقل إلى الصفحة الرئيسية مباشرة

## 🔧 التنفيذ التقني

### الملفات الجديدة/المعدلة:

1. **`app/api/auth/demo-login/route.ts`** (جديد)
   - API endpoint لتسجيل الدخول التجريبي
   - يتعامل مع المصادقة والـ cookies

2. **`components/auth/login-form.tsx`** (معدل)
   - إضافة دالة `handleDemoLogin`
   - إضافة زر "تسجيل دخول تجريبي"

3. **`scripts/setup-demo-user.js`** (جديد)
   - script لإنشاء حساب Demo تلقائياً
   - يستخدم Supabase Admin API

4. **`scripts/create-demo-user.sh`** (جديد)
   - instructions يدويات لإنشاء الحساب

## 🚀 الاستخدام السريع

```bash
# 1. إعداد البيئة
export NEXT_PUBLIC_SUPABASE_URL=your_url
export SUPABASE_SERVICE_ROLE_KEY=your_key

# 2. إنشاء حساب Demo
node scripts/setup-demo-user.js

# 3. تشغيل التطبيق
npm run dev

# 4. اذهب إلى http://localhost:3000/login
# وانقر على "تسجيل دخول تجريبي"
```

## ✅ الميزات

✓ تسجيل دخول فوري بدون الحاجة لإدخال بيانات
✓ حساب Demo آمن ومحدود الصلاحيات
✓ يدعم الواجهة العربية
✓ يعمل مع نفس نظام المصادقة الحالي
✓ يمكن حذف الحساب أو تعديله في أي وقت

## ⚠️ ملاحظات أمنية

- ألا تستخدم بيانات Demo في الإنتاج الفعلي
- يمكنك تغيير كلمة المرور من الإعدادات بعد تسجيل الدخول
- حساب Demo يجب أن يكون للاختبار والتطوير فقط
- في الإنتاج، ضع هذا الزر خلف علم (flag) أو أخفه تماماً

## 🔐 تغيير البيانات

إذا أردت تغيير بيانات Demo:

1. عدّل في `app/api/auth/demo-login/route.ts`:
```typescript
const DEMO_EMAIL = "your-demo@email.com"
const DEMO_PASSWORD = "your-password"
```

2. عدّل في `scripts/setup-demo-user.js`:
```javascript
const DEMO_EMAIL = "your-demo@email.com"
const DEMO_PASSWORD = "your-password"
```

3. أعد إنشاء الحساب من Supabase Dashboard
