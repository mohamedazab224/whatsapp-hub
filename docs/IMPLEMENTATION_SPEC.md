# وثيقة المواصفات التنفيذية - WhatsApp Hub

تاريخ الإنشاء: 7 فبراير 2026  
الإصدار: 1.0  
الحالة: مستند من الواجهة الفعلية

---

## (1) خريطة التنقل (Navigation Map)

### القائمة الجانبية الرئيسية

```
نظام العزب (Alazab-System)
│
├── لوحة المعلومات (Dashboard) - Route: /
├── أضف رقماً جديداً (Add New Number) - Route: /numbers/add
├── صندوق الوارد (Inbox) - Route: /inbox ✓ نشط في الصورة A
│
├── التكامل (Integration Section)
│   ├── أرقام الهواتف (Phone Numbers) - تتفرع إلى:
│   │   ├── الأرقام المتصلة - Route: /numbers/connected ✓ ظاهر في الصورة D
│   │   ├── الأرقام الرقمية - Route: /numbers/digital ✓ ظاهر في الصورة E
│   │   └── اختبار صندوق الرمل - Route: /numbers/sandbox ✓ ظاهر في الصورة F
│   ├── ويبهوكس (Webhooks) - Route: /webhooks
│   ├── API Keys - Route: /api-keys
│   ├── تدفقات واتساب (WhatsApp Flows) - Route: /flows ✓ ظاهر في الصورة C
│   └── القوالب (Templates) - Route: /templates ✓ ظاهر في الصورة B
│
├── المستخدمون (Users Section) - قابل للتوسيع
│   ├── السجلات (Logs)
│   └── [عناصر أخرى غير ظاهرة]
│
├── رائب (Reports/Analytics)
├── وثائق (Docs) - Route: /docs
├── المساعدة (Help) - Route: /help
└── صندوق وارد الفريق (Team Inbox) - Route: /team-inbox
```

### الشاشات المحددة من الصور

| الشاشة | Route | الصورة المرجعية |
|--------|-------|------------------|
| صندوق الوارد | `/inbox` | A - يعرض محادثات مع "يد محمد154" |
| القوالب | `/templates` | B - يعرض 4 قوالب تحت الموافقة |
| تدفقات واتساب | `/flows` | C - حالة فارغة |
| الأرقام المتصلة | `/numbers/connected` | D - يعرض رقمين (إنتاج + صندوق رمل) |
| الأرقام الرقمية | `/numbers/digital` | E - حالة فارغة |
| رقم صندوق الرمل | `/numbers/sandbox` | F - يعرض رقم +56920403095 |
| جهات اتصال واتساب | `/whatsapp/contacts` | G - 112 جهة اتصال |
| واتساب ميديا | `/whatsapp/media` | H - قائمة الملفات المرفوعة |
| لوحة المعلومات | `/` | I - إحصائيات ورسوم بيانية |

---

## (2) مواصفات كل شاشة (Screen Specifications)

### الشاشة A: صندوق الوارد (Inbox)

**الهدف الوظيفي:**  
عرض جميع المحادثات النشطة مع العملاء عبر واتساب، مع إمكانية فلترة المحادثات والرد عليها في الوقت الفعلي.

**عناصر الواجهة:**

#### الجزء الأيسر (قائمة المحادثات):
- **العنوان:** "صندوق الوارد"
- **أزرار التحكم العلوية:**
  - أيقونة تثبيت (دبوس)
  - Toggle تفعيل/تعطيل
  - أيقونة تعديل
- **شريط البحث:** "ابحث عن أرقام الهواتف..."
- **Dropdown اختيار الموظف:** "جميع الموكلين لي..." مع أيقونة مجموعة
- **Dropdown الفلترة:** "نشط" (يمكن اختيار حالات أخرى)

#### قائمة المحادثات (3 محادثات ظاهرة):

1. **محمد سامي** (MS - دائرة برتقالية)
   - الحالة: "نشط" (شارة خضراء)
   - النص: "منذ حوالي ساعة واحدة" / "مابي بارد عادي ليه با كريم"
   - التاريخ: "محدد عرب"

2. **يد محمد154** (HM - دائرة فيروزية) ✓ محدد حالياً
   - الحالة: "نشط" (شارة خضراء)
   - النص: "منذ حوالي 6 ساعات" / "صورة مرفقة (image_cd37814e)"
   - التاريخ: "محدد عرب"

3. **م محمود سعد** (AM - دائرة زرقاء)
   - النص: "منذ حوالي 10 ساعات" / "حوالي فلوس شغل 5000"
   - التاريخ: "محدد عرب"

#### الجزء الأوسط (منطقة المحادثة):
- **رأس المحادثة:**
  - الاسم: "يد محمد154"
  - الرقم: "+201099884670"
  - التوقيت: "نشط • منذ حوالي 6 ساعات"
  - أيقونة الدبوس في الزاوية اليسرى
  
- **جسم المحادثة:**
  - رسائل صادرة ووارده
  - دعم الصور المرفقة (3 صور ظاهرة لمطبخ)
  - طوابع زمنية: "10:35 م" و "10:38 م"
  - حالة التسليم: علامة صح مزدوجة ✓✓

- **منطقة الرد:**
  - حقل إدخال النص: "اكتب رسالتك..."
  - أيقونة إضافة مرفقات (+)
  - زر الإرسال (أيقونة طائرة ورقية خضراء)
  - نص تحذيري: "الوضع اليدوي" (مع أيقونة معلومات)

#### الجزء الأيمن (بطاقة العميل):
- **تبويبات:** "معلومات" (نشط) و "معلومات"
- **معلومات العميل:**
  - الاسم: "يد محمد154"
  - الرقم: 201099884670
  - رابط "عرض المحادثات"
  - زر "+ إضافة ملاحظة"
  
- **قسم التعيين:**
  - عنوان: "التعيين" مع أيقونة المستخدمين
  - النص: "لم يتم تعريفيه"
  
- **قسم الحالة:**
  - شارة خضراء: "نشط"
  
- **قسم النشاط:**
  - "آخر مرة نشاط: منذ حوالي 6 ساعات"
  - "إجمالي الرسائل: 165"

**البيانات المعروضة:**
- عدد المحادثات: 3 محادثات ظاهرة (من إجمالي غير معروف)
- أنواع الرسائل: نصية + صور
- حالات المحادثة: "نشط" / "محدد عرب"
- الطوابع الزمنية: نسبية ("منذ حوالي X ساعات") ومطلقة ("10:35 م")

**الأفعال (User Actions):**
- **البحث:** فلترة المحادثات بالرقم أو الاسم
- **اختيار محادثة:** عرض تفاصيل المحادثة في الوسط
- **الفلترة:** حسب الحالة (نشط/مغلق/...) أو الموكل
- **التثبيت:** تثبيت محادثة مهمة
- **إضافة مرفق:** إرفاق صورة/ملف
- **إرسال رسالة:** كتابة والضغط على زر الإرسال
- **إضافة ملاحظة:** توثيق معلومات إضافية عن العميل
- **عرض المحادثات:** الانتقال إلى سجل المحادثات الكامل

**حالات النظام:**
- **عادي:** يعرض المحادثات
- **Empty State:** غير ظاهر
- **Loading:** غير ظاهر
- **Error:** غير ظاهر

**قواعد العرض:**
- RTL (من اليمين لليسار)
- الرسائل الصادرة: محاذاة يمين، خلفية خضراء
- الرسائل الواردة: محاذاة يسار، خلفية رمادية
- التواريخ: نسبية ("منذ X")
- الأرقام: بصيغة دولية +20...
- الصور: معاينة مصغرة قابلة للنقر

---

### الشاشة B: القوالب (Templates)

**الهدف الوظيفي:**  
إدارة قوالب رسائل واتساب المعتمدة من Meta، مع إمكانية الإنشاء والمزامنة والمعاينة.

**عناصر الواجهة:**

#### الرأس:
- **العنوان:** "قوالب واتساب"
- **الوصف:** "إدارة قوالب الرسائل على واتساب"
- **أزرار:**
  - زر "إنشاء تدفق +" (أخضر)
  - زر "مزامنة من ميتا" (رمادي مع أيقونة تحديث)

#### منطقة الفلترة:
- **Dropdown اختيار الحساب:** "محمد عزاب +20…" مع رقم الهاتف
- **شريط بحث:** "قوالب البحث..."
- **Dropdown الحالة:** "جميع الحالات"
- **Dropdown الفئة:** "جميع الفئات"

#### التبويبات:
- "التدفقات" (نشط) ✓
- "الردود"

#### الجدول:
**الأعمدة (من اليمين لليسار):**
1. الاسم
2. الحالة
3. الفئة
4. اللغة
5. معاينة

**الصفوف الظاهرة (4 قوالب):**

1. **technician_assigned**
   - الحالة: شارة خضراء "تحت الموافقة"
   - الفئة: الفائدة
   - اللغة: AR
   - المعاينة: "تم تعيين فني لطلب الصيانة الخاص بك! رقم الطلب: {{order_id}}..."
   - الإجراءات: أيقونة إرسال + قائمة ثلاث نقاط

2. **booking_confirmation**
   - الحالة: شارة خضراء "تحت الموافقة"
   - الفئة: الفائدة
   - اللغة: AR
   - المعاينة: "تم تأكيد حجزك بنجاح ✅ تفاصيل الحجز: رقم الطلب: {{number}}..."
   - الإجراءات: أيقونة إرسال + قائمة ثلاث نقاط

3. **order_created**
   - الحالة: شارة خضراء "تحت الموافقة"
   - الفئة: الفائدة
   - اللغة: AR
   - المعاينة: "تم استلام طلب الصيانة بنجاح. رقم الطلب: {{order_id}}..."
   - الإجراءات: أيقونة إرسال + قائمة ثلاث نقاط

4. **test_service**
   - الحالة: شارة خضراء "تحت الموافقة"
   - الفئة: التسويق
   - اللغة: AR
   - المعاينة: "مرحباً {{customer_name}} هذه رسالة اختبار للتأكد من عمل..."
   - الإجراءات: أيقونة إرسال + قائمة ثلاث نقاط

**البيانات المعروضة:**
- عدد القوالب: 4 قوالب ظاهرة
- الحالات المتاحة: "تحت الموافقة"
- الفئات: "الفائدة"، "التسويق"
- اللغات: "AR" (العربية)
- المتغيرات: `{{order_id}}`, `{{number}}`, `{{customer_name}}`

**الأفعال (User Actions):**
- **إنشاء تدفق:** فتح نموذج إنشاء قالب جديد
- **مزامنة من ميتا:** جلب أحدث القوالب من Meta Business
- **تغيير الحساب:** عرض قوالب حساب واتساب مختلف
- **البحث:** فلترة القوالب بالاسم
- **فلترة الحالة:** عرض قوالب حسب الحالة (معتمد/مرفوض/قيد المراجعة)
- **فلترة الفئة:** عرض قوالب حسب النوع (تسويق/فائدة/مصادقة)
- **إرسال قالب:** إرسال رسالة باستخدام هذا القالب
- **قائمة الإجراءات:** تعديل/حذف/تكرار القالب

**حالات النظام:**
- **عادي:** يعرض قائمة القوالب ✓
- **Empty State:** "لا توجد بيانات متاحة" (ظاهر في تبويب "الردود")
- **Loading:** غير ظاهر
- **Syncing:** غير ظاهر

**قواعد العرض:**
- RTL
- الحالات: شارات ملونة (أخضر = تحت الموافقة)
- المعاينة: نص مختصر مع "..." + متغيرات بصيغة `{{variable}}`
- الأيقونات: موحدة (إرسال، تعديل، قائمة)

---

### الشاشة C: تدفقات واتساب (WhatsApp Flows)

**الهدف الوظيفي:**  
إدارة تدفقات واتساب التفاعلية (Forms) التي تسمح بجمع بيانات منظمة من المستخدمين.

**عناصر الواجهة:**

#### الرأس:
- **العنوان:** "تدفقات واتساب"
- **الوصف:** "يتيح لك تدفقات واتساب إنشاء نماذج تفاعلية وجمع بيانات منظمة من المستخدمين. تعرف على المزيد" (مع رابط)
- **أزرار:**
  - زر "إنشاء تدفق +" (أخضر)
  - زر "مزامنة من ميتا" (رمادي مع أيقونة تحديث)

#### التبويبات:
- "التدفقات" (نشط) ✓
- "الردود"

#### منطقة المحتوى:
- **حالة فارغة (Empty State):**
  - إطار كبير بحدود منقطة
  - النص: "لا توجد بيانات متاحة"
  - (لا يوجد أيقونة أو CTA إضافي)

**البيانات المعروضة:**
- عدد التدفقات: 0 (حالة فارغة)

**الأفعال (User Actions):**
- **إنشاء تدفق:** فتح محرر التدفقات لإنشاء Form جديد
- **مزامنة من ميتا:** جلب التدفقات من Meta Business Manager
- **تعرف على المزيد:** فتح توثيق WhatsApp Flows

**حالات النظام:**
- **Empty State:** ظاهر ✓
- **عادي (مع بيانات):** غير ظاهر
- **Loading:** غير ظاهر
- **Error:** غير ظاهر

**قواعد العرض:**
- RTL
- حالة فارغة: إطار كبير مع نص مركزي
- الروابط: باللون الأزرق مع أيقونة رابط خارجي

---

### الشاشة D: جهات اتصال واتساب (WhatsApp Contacts)

**الهدف الوظيفي:**  
عرض وإدارة جميع جهات الاتصال التي تواصلت عبر واتساب، مع إمكانية البحث والفلترة والترقيم الصفحي.

**عناصر الواجهة:**

#### الرأس:
- **العنوان:** "جهات اتصال واتساب"
- **التوجيه:** "إدارة جهات الاتصال واتساب" (أعلى يمين)

#### منطقة البحث:
- **شريط بحث:** "ابحث بالهاتف أو بالاسم..."

#### الجدول:
**الأعمدة (من اليمين لليسار):**
1. معرف واتساب
2. اسم العرض
3. تأسيس
4. الإجراءات

**صفوف العينة (20 صف ظاهر من أصل 112):**

| المعرف | اسم العرض | تأسيس | الإجراءات |
|--------|-----------|-------|-----------|
| 201804093405 | يوسف أكرم<br>واتساب: يوسف أكرم | منذ 3 أيام | 👁 |
| 201800159529 | 9529 015 108 +20<br>واتساب: 9529 015 108 +20 | منذ 7 أيام | 👁 |
| 201096435460 | مستشفى حالة ت<br>واتساب: مستشفى حالة ت | منذ 8 أيام | 👁 |
| 201151955131 | شدوى يوسف<br>واتساب: شدوى يوسفة | منذ 11 يوماً | 👁 |
| 201095660091 | حازم دومیار<br>واتساب: حازم دومیار | منذ 17 يوماً | 👁 |
| 966507790002 | ابراهيم فالح<br>واتساب: ابراهيم فالح | منذ 18 يوماً | 👁 |

... والمزيد حتى 20 صف

#### Pagination (التنقل بين الصفحات):
- النص: "1 - 20 من أصل 112 مشاركة"
- Dropdown: "لكل صفحة" مع القيمة 20
- أزرار الصفحات: « ‹ 1 2 3 4 5 6 › »
- الصفحة الحالية: 1 (باللون الأخضر)

**البيانات المعروضة:**
- إجمالي جهات الاتصال: 112
- جهات الاتصال في الصفحة الحالية: 20
- المعلومات لكل جهة اتصال:
  - رقم الهاتف (بدون +)
  - الاسم المعروض (قد يكون مطابق للرقم)
  - اسم واتساب (قد يختلف عن الاسم المعروض)
  - تاريخ التأسيس النسبي

**الأفعال (User Actions):**
- **البحث:** فلترة جهات الاتصال بالرقم أو الاسم
- **عرض التفاصيل (👁):** فتح صفحة تفاصيل جهة الاتصال
- **تغيير الصفحة:** التنقل بين الصفحات (1-6)
- **تغيير حجم الصفحة:** عرض 10/20/50/100 جهة اتصال

**حالات النظام:**
- **عادي:** يعرض قائمة جهات الاتصال ✓
- **Empty State:** غير ظاهر
- **Loading:** غير ظاهر
- **Searching:** غير ظاهر

**قواعد العرض:**
- RTL
- الأرقام: بدون + (201804093405)
- التواريخ: نسبية ("منذ X أيام/يوماً")
- الجدول: تصميم نظيف مع حدود خفيفة
- Pagination: أسفل الجدول مع تمييز الصفحة الحالية

---

### الشاشة E: واتساب ميديا (WhatsApp Media)

**الهدف الوظيفي:**  
عرض وإدارة جميع الملفات المرفقة المرسلة والمستقبلة عبر واتساب (صور، فيديو، مستندات).

**عناصر الواجهة:**

#### الرأس:
- **العنوان:** "واتساب ميديا"

#### التبويبات:
- "اليوم" ✓ (نشط)
- "هذا الأسبوع"
- "هذا الشهر"
- "الكل"
- أيقونة تقويم (لاختيار نطاق زمني مخصص)

#### القائمة الجانبية (فلترة):
- **نوع المعلومات:** مع Dropdown
  - أضف رقماً جديداً
  - صندوق الوارد
- **التكامل:**
  - أرقام الهواتف (موسع) ✓
    - الأرقام المتصلة
    - الأرقام الرقمية
    - اختبار صندوق الرمل
  - ويبهوكس
  - مفاتيح واجهة برمجة التطبيقات
  - تدفقات واتساب
  - القوالب
- **المستخدمون:** (موسع)
- **رائب:** (موسع)
- **وثائق**
- **المساعدة**
- **صندوق وارد الفريق**
- الحساب: mohamed@alazab.com (أسفل القائمة)

#### الجدول:
**الأعمدة (من اليمين لليسار):**
1. الزمن
2. المحتوى
3. الحجم
4. النوع
5. الاتصال
6. الهاتف
7. الحالة
8. الإجراءات

**صفوف العينة (حوالي 50+ صف ظاهر):**

| ID | الاسم | النوع | الحجم | التاريخ | الإجراءات |
|-----|------|-------|-------|---------|-----------|
| 452929026 | image_cd37814e... | الصور | 93.1 KB | 6 فبراير، 11:28 م | 👁 📋 ⬇ 🗑 |
| 456d4615 | image_42366d26... | الصور | 67.3 KB | 6 فبراير، 06:40 م | 👁 📋 ⬇ 🗑 |
| 158787553 | image_01838343... | الصور | 48.4 KB | 6 فبراير، 06:40 م | 👁 📋 ⬇ 🗑 |
| ... | ... | ... | ... | ... | ... |

(العديد من الصور والفيديوهات بأحجام مختلفة)

#### Pagination:
- النص: "1 - 50 من حوالي 1716 مشاركة"
- أزرار الصفحات: « ‹ 1 2 3 4 5 ... 16 › »
- الصفحة الحالية: 1 (باللون الأخضر)

**البيانات المعروضة:**
- إجمالي الملفات: حوالي 1716
- الملفات في الصفحة الحالية: 50
- أنواع الملفات:
  - صور: image_xxx.jpeg/png
  - فيديو: video_xxx.mp4
- الأحجام: من 2.1 MB إلى 797.2 KB
- الحالة: "موافق" (شارة خضراء)

**الأفعال (User Actions):**
- **معاينة (👁):** عرض الملف في نافذة منبثقة
- **نسخ (📋):** نسخ رابط الملف
- **تنزيل (⬇):** تحميل الملف
- **حذف (🗑):** حذف الملف من النظام
- **فلترة بالتاريخ:** عرض ملفات اليوم/الأسبوع/الشهر
- **التنقل بين الصفحات:** استعراض المزيد من الملفات

**حالات النظام:**
- **عادي:** يعرض قائمة الملفات ✓
- **Empty State:** غير ظاهر
- **Loading:** غير ظاهر
- **Error:** غير ظاهر

**قواعد العرض:**
- RTL
- الأحجام: بصيغة KB/MB
- التواريخ: مطلقة (6 فبراير، 06:40 م)
- الأسماء: معرفات فريدة (image_cd37814e)
- الحالة: شارات خضراء "موافق"
- الأيقونات: موحدة وملونة

---

### الشاشة F: الأرقام المتصلة (Connected Numbers)

**الهدف الوظيفي:**  
عرض وإدارة أرقام واتساب المتصلة بالنظام (للإنتاج وصندوق الرمل).

**عناصر الواجهة:**

#### الرأس:
- **العنوان:** "الأرقام المتصلة"
- **المؤشر:** "1 / 1 أرقام هواتف"
- **الوصف:** "إدارة اتصالات واجهة تطبيقات واتساب للأعمال"
- **زر:** "الترقية لإضافة المزيد" (أخضر)

#### التنبيه المعلوماتي:
- أيقونة معلومات (i)
- النص: "لقد وصلت إلى حد رقم هاتفك (1). قم بترقية خطتك لإضافة المزيد من إعدادات واتساب."

#### منطقة الفلترة:
- **شريط بحث:** "اسم البحث، الهاتف، الزون..."
- **Dropdown:** "جميع الأنواع"

#### البطاقات (Cards):

**البطاقة 1: محمد عزاب** (الإنتاج)
- **شارة:** "الإنتاج" (خضراء)
- **المعلومات:**
  - المعرف الافتراضي
  - الرقم: +20… (غير مكتمل)
  - المعرف: 873908352481274
- **الإجراءات:**
  - زر "فحص الصحة" (مع أيقونة نبض القلب)
  - أيقونة تعديل (قلم)
  - قائمة ثلاث نقاط

**البطاقة 2: واتساب صندوق الرمل**
- **شارة:** "صندوق الرمل" (رمادية)
- **المعلومات:**
  - صندوق الرمل
- **الإجراءات:**
  - أيقونة تعديل (قلم)
  - قائمة ثلاث نقاط

**البيانات المعروضة:**
- عدد الأرقام: 2 (1 إنتاج + 1 صندوق رمل)
- الحد الأقصى: 1 رقم إنتاج (حسب الخطة)
- أنواع الأرقام: "الإنتاج" / "صندوق الرمل"

**الأفعال (User Actions):**
- **الترقية:** فتح صفحة الاشتراكات لزيادة عدد الأرقام
- **البحث:** فلترة الأرقام بالاسم/الرقم
- **فلترة النوع:** عرض أرقام معينة (إنتاج/صندوق رمل)
- **فحص الصحة:** اختبار اتصال الرقم مع WhatsApp API
- **تعديل:** تحديث إعدادات الرقم
- **قائمة الإجراءات:** حذف/تعطيل/نسخ المعرف

**حالات النظام:**
- **عادي:** يعرض الأرقام المتصلة ✓
- **حد مكتمل:** تنبيه معلوماتي ✓
- **Empty State:** غير ظاهر
- **Loading:** غير ظاهر

**قواعد العرض:**
- RTL
- البطاقات: تصميم Card مع شارات ملونة
- التنبيهات: خلفية زرقاء فاتحة مع أيقونة
- الشارات: ألوان مختلفة (أخضر=إنتاج، رمادي=صندوق رمل)

---

### الشاشة G: رقم صندوق الرمل (Sandbox Number)

**الهدف الوظيفي:**  
إعداد واختبار واتساب باستخدام رقم صندوق رمل مؤقت قبل الانتقال للإنتاج.

**عناصر الواجهة:**

#### الرأس:
- **العنوان:** "رقم صندوق الرمل"
- **شارة:** "ما هذا؟" (زر معلومات صغير)

#### بطاقة المعلومات:
- **الرقم:** +56920403095
- **الروابط:**
  - "واتساب" (رابط مع أيقونة واتساب)
  - "رسالة على واتساب" (رابط مع أيقونة)
- **التوجيه:**
  - "أضف رقم هاتفك أثناء البدء في إرسال الرسائل النصية برقم صندوق الرمل"
- **زر CTA:** "+ ابدأ جلسة الاختبار" (أخضر)

#### الجدول:
**الأعمدة (من اليمين لليسار):**
1. رقم الهاتف
2. التكوين
3. الحالة
4. رمز التفعيل
5. تم تفعيله
6. تأسيس
7. الإجراءات

**الصف الظاهر:**

| رقم الهاتف | التكوين | الحالة | رمز التفعيل | تم تفعيله | تأسيس | الإجراءات |
|-----------|---------|--------|-------------|-----------|-------|-----------|
| 201092750351 | صندوق الرمل | "قيد التفعيل" (شارة خضراء) | MPTYQU<br>(مع رابط "مفتوح") | ليس بعد | منذ حوالي شهر | 🗑 (أحمر) |

**البيانات المعروضة:**
- رقم صندوق الرمل: +56920403095
- الأرقام المربوطة: 1 (201092750351)
- رمز التفعيل: MPTYQU
- الحالة: "قيد التفعيل"
- التفعيل: "ليس بعد"

**الأفعال (User Actions):**
- **ما هذا؟:** عرض شرح عن صندوق الرمل
- **فتح واتساب:** فتح محادثة مع رقم صندوق الرمل
- **رسالة على واتساب:** فتح رابط wa.me
- **ابدأ جلسة الاختبار:** إضافة رقم جديد للاختبار
- **مفتوح (رمز التفعيل):** نسخ الرمز أو عرض التعليمات
- **حذف (🗑):** إزالة الرقم من صندوق الرمل

**حالات النظام:**
- **عادي:** يعرض رقم صندوق الرمل والأرقام المربوطة ✓
- **قيد التفعيل:** شارة خضراء ✓
- **Empty State:** غير ظاهر (يوجد صف واحد)
- **مفعّل:** غير ظاهر ("ليس بعد")

**قواعد العرض:**
- RTL
- رقم صندوق الرمل: بصيغة دولية (+56...)
- الأرقام المربوطة: بدون + (201092750351)
- رمز التفعيل: أحرف كبيرة (MPTYQU) مع رابط
- الحالات: شارات ملونة
- التواريخ: نسبية ("منذ حوالي شهر")

---

### الشاشة H: لوحة المعلومات (Dashboard)

**الهدف الوظيفي:**  
عرض ملخص شامل لإحصائيات المشروع ونشاط الرسائل والأداء العام.

**عناصر الواجهة:**

#### الرأس:
- **العنوان:** "لوحة المعلومات"
- **التبويبات:**
  - "مجاني" ✓ (نشط)
  - "الربح"

#### منتقي الفترة الزمنية:
- **الأزرار:**
  - "اليوم"
  - "هذا الأسبوع"
  - "هذا الشهر"
  - أيقونة تقويم (لاختيار نطاق مخصص)
- **النطاق المختار:** "1 فبراير - 7 فبراير، 2026"

#### بطاقات الإحصائيات (4 بطاقات):

1. **سير العمل**
   - القيمة: 0 نقاط
   - الوصف: حقيقياً يا نجمة
   - أيقونة: >

2. **السجلات**
   - القيمة: 71 خطأ
   - الوصف: من 490 إجمالي
   - أيقونة: >

3. **الرسائل**
   - القيمة: 235
   - الوصف: من 71 في + 164 خارج
   - أيقونة: >

4. **الأرقام**
   - القيمة: 1
   - الوصف: 1 النشاش
   - أيقونة: >

#### الرسم البياني (الرسائل اليومية):
- **العنوان:** "الرسائل اليومية"
- **الوصف:** "إعراضات سريعة"
- **المحاور:**
  - المحور الصادي (Y): من 0 إلى 80
  - المحور الأفقي (X): التواريخ (31 يناير، 1 فبراير، 2 فبراير، 3 فبراير، 4 فبراير، 5 فبراير، 6 فبراير)
- **البيانات:**
  - أعمدة خضراء (الوارد): تظهر قيم متفاوتة، أعلاها في 3 فبراير (~80 رسالة)
  - أعمدة فيروزية (الخارج): تظهر قيم أقل
- **المفتاح:**
  - دائرة خضراء: "الوارد"
  - دائرة فيروزية: "الخارج"

#### بطاقات الإجراءات السريعة:

1. **أضف رقم الهاتف**
   - الأيقونة: هاتف محمول (أخضر)
   - الوصف: "نحن نوفرها لك"

2. **إنشاء webhook**
   - الأيقونة: ويب هوك (رمادي)
   - الوصف: "احصل على إخطارات على أرصالك"

3. **إنشاء مفتاح API**
   - الأيقونة: مفتاح (رمادي)
   - الوصف: "تكامل عبر واجهة برمجة التطبيقات..."

4. **إنشاء سير العمل**
   - الأيقونة: سير عمل (رمادي)
   - الوصف: "وكلاء الذكاء الاصطناعي وأداء..."

#### الوثائق (Docs):
- **العنوان:** "الوثائق"
- **النص:** "بالنسبة لوحة الذكاء الاصطناعي، قم بتثبيت مهارات كابسو:"
- **الأزرار:**
  - "SDK Node.js" (مع أيقونة نسخ)
  - "الأوكتوبس" (Dropdown)
  - "الموثـر"
- **أمثلة الكود:**
```
*=*cpServers*: { *kapso*: { *url*: *https://docs.kapso.ai/mc'go'* } } }
```

#### الرسائل الحديثة:
- **العنوان:** "الرسائل الحديثة"
- **الرابط:** "عرض صندوق الوارد"
- **الجدول:**

| الزمن | المحتوى | الإعراض | الحالة | الاتصال | الهاتف |
|-------|---------|---------|--------|---------|--------|
| 6 فبراير، 11:28 م | منش رجل علية ليه با كريم | — | — | محمد سامي | 201095663003 |
| 6 فبراير، 06:40 م | صورة مرفقة (image_37814e5f3b.jpeg) | إرسل ⬇ | الخارج | يد محمد154 | 201099884670 |
| 6 فبراير، 06:40 م | صورة مرفقة (image_3d66d0260b.jpeg) | إرسل ⬇ | الخارج | يد محمد154 | 201099884670 |
| 6 فبراير، 06:40 م | صورة مرفقة (image_38384320e9.jpeg) | إرسل ⬇ | الخارج | يد محمد154 | 201099884670 |
| ... | ... | ... | ... | ... | ... |

(حوالي 10 صفوف ظاهرة)

#### Pagination:
- النص: "10 - 1 من أصل 20 مشاركة"
- أزرار: « ‹ 1 2 › »

**البيانات المعروضة:**
- عدد الأرقام: 1 (نشط)
- إجمالي الرسائل: 235 (71 واردة + 164 صادرة)
- السجلات: 71 خطأ من 490
- سير العمل: 0
- الرسائل اليومية: بيانات 7 أيام
- الرسائل الحديثة: 20 رسالة

**الأفعال (User Actions):**
- **اختيار الفترة:** تغيير نطاق البيانات المعروضة
- **عرض التفاصيل:** النقر على بطاقة إحصائية للانتقال للقسم المفصل
- **أضف رقم:** بدء عملية إضافة رقم جديد
- **إنشاء webhook:** فتح نموذج إنشاء webhook
- **إنشاء مفتاح API:** توليد API key جديد
- **إنشاء سير العمل:** بدء بناء workflow
- **نسخ الكود:** نسخ مثال الكود
- **عرض صندوق الوارد:** الانتقال إلى صفحة الرسائل الكاملة
- **إرسال (⬇):** تنزيل الرسالة/المرفق

**حالات النظام:**
- **عادي:** يعرض الإحصائيات والبيانات ✓
- **Empty State:** غير ظاهر
- **Loading:** غير ظاهر
- **Error في السجلات:** 71 خطأ ✓

**قواعد العرض:**
- RTL
- البطاقات: تصميم Card مع أيقونات ملونة
- الرسم البياني: أعمدة عمودية (Bar Chart) مع مفتاح
- الألوان: أخضر للوارد، فيروزي للخارج
- التواريخ: مطلقة (6 فبراير، 06:40 م)
- الأرقام: بصيغة محلية (201099884670)

---

## (3) نماذج البيانات (Data Models)

### 3.1 WhatsAppContact

```typescript
interface WhatsAppContact {
  id: string;                    // المعرف الفريد
  phone_number: string;          // رقم الهاتف (201804093405)
  display_name: string;          // اسم العرض
  whatsapp_name?: string;        // الاسم في واتساب (قد يختلف)
  profile_picture_url?: string;  // صورة الملف الشخصي
  created_at: string;            // تاريخ الإنشاء (ISO 8601)
  last_message_at?: string;      // آخر رسالة
  tags?: string[];               // غير ظاهر في الصور
  notes?: string;                // غير ظاهر في الصور
  project_id: string;            // معرف المشروع
}

// مثال من الشاشة G:
{
  "id": "uuid-1",
  "phone_number": "201804093405",
  "display_name": "يوسف أكرم",
  "whatsapp_name": "يوسف أكرم",
  "created_at": "2026-02-04T00:00:00Z"  // "منذ 3 أيام"
}
```

### 3.2 WhatsAppMessage

```typescript
interface WhatsAppMessage {
  id: string;                    // المعرف الفريد
  conversation_id: string;       // معرف المحادثة
  contact_id: string;            // معرف جهة الاتصال
  direction: 'inbound' | 'outbound'; // الاتجاه
  type: 'text' | 'image' | 'video' | 'document' | 'audio'; // النوع
  content?: string;              // محتوى نصي
  media_url?: string;            // رابط الوسائط
  media_id?: string;             // معرف الوسائط (image_cd37814e)
  status: 'sent' | 'delivered' | 'read' | 'failed'; // الحالة
  timestamp: string;             // الطابع الزمني (ISO 8601)
  error_message?: string;        // رسالة الخطأ
  whatsapp_message_id?: string;  // معرف واتساب
  project_id: string;            // معرف المشروع
}

// مثال من الشاشة A:
{
  "id": "msg-1",
  "conversation_id": "conv-154",
  "contact_id": "201099884670",
  "direction": "outbound",
  "type": "image",
  "media_id": "image_cd37814e",
  "media_url": "https://...",
  "status": "read",
  "timestamp": "2026-02-06T22:35:00Z"  // "10:35 م"
}
```

### 3.3 Conversation

```typescript
interface Conversation {
  id: string;                    // المعرف الفريد
  contact_id: string;            // معرف جهة الاتصال
  whatsapp_number_id: string;    // الرقم المستخدم للمحادثة
  status: 'active' | 'closed' | 'archived'; // الحالة
  last_message_at: string;       // آخر رسالة (ISO 8601)
  last_message_preview: string;  // معاينة آخر رسالة
  unread_count: number;          // عدد الرسائل غير المقروءة
  assigned_to?: string;          // معرف الموظف المعين
  tags?: string[];               // الوسوم
  total_messages: number;        // إجمالي الرسائل
  created_at: string;            // تاريخ الإنشاء
  project_id: string;            // معرف المشروع
}

// مثال من الشاشة A:
{
  "id": "conv-154",
  "contact_id": "201099884670",
  "status": "active",
  "last_message_at": "2026-02-06T16:00:00Z",  // "منذ حوالي 6 ساعات"
  "last_message_preview": "صورة مرفقة (image_cd37814e)",
  "unread_count": 0,
  "total_messages": 165
}
```

### 3.4 WhatsAppTemplate

```typescript
interface WhatsAppTemplate {
  id: string;                    // المعرف الفريد
  name: string;                  // اسم القالب (technician_assigned)
  category: 'UTILITY' | 'MARKETING' | 'AUTHENTICATION'; // الفئة
  language: string;              // اللغة (ar, en, etc.)
  status: 'PENDING' | 'APPROVED' | 'REJECTED'; // الحالة
  components: TemplateComponent[]; // مكونات القالب
  variables?: string[];          // المتغيرات (order_id, customer_name)
  preview_text: string;          // نص المعاينة
  whatsapp_template_id?: string; // معرف واتساب
  created_at: string;            // تاريخ الإنشاء
  updated_at: string;            // تاريخ التحديث
  synced_at?: string;            // آخر مزامنة من Meta
  project_id: string;            // معرف المشروع
  whatsapp_number_id: string;    // الرقم المرتبط
}

interface TemplateComponent {
  type: 'HEADER' | 'BODY' | 'FOOTER' | 'BUTTONS';
  format?: 'TEXT' | 'IMAGE' | 'VIDEO' | 'DOCUMENT';
  text?: string;
  example?: string[];
  buttons?: TemplateButton[];
}

interface TemplateButton {
  type: 'QUICK_REPLY' | 'URL' | 'PHONE_NUMBER';
  text: string;
  url?: string;
  phone_number?: string;
}

// مثال من الشاشة B:
{
  "id": "tmpl-1",
  "name": "technician_assigned",
  "category": "UTILITY",
  "language": "ar",
  "status": "PENDING",
  "preview_text": "تم تعيين فني لطلب الصيانة الخاص بك! رقم الطلب: {{order_id}}...",
  "variables": ["order_id"],
  "created_at": "2026-01-15T00:00:00Z"
}
```

### 3.5 WhatsAppFlow

```typescript
interface WhatsAppFlow {
  id: string;                    // المعرف الفريد
  name: string;                  // اسم التدفق
  status: 'DRAFT' | 'PUBLISHED'; // الحالة
  json_schema: object;           // هيكل النموذج (JSON)
  whatsapp_flow_id?: string;     // معرف واتساب
  created_at: string;            // تاريخ الإنشاء
  updated_at: string;            // تاريخ التحديث
  synced_at?: string;            // آخر مزامنة من Meta
  project_id: string;            // معرف المشروع
}

// ملاحظة: حالة فارغة في الشاشة C - لا توجد بيانات حقيقية
```

### 3.6 FlowResponse

```typescript
interface FlowResponse {
  id: string;                    // المعرف الفريد
  flow_id: string;               // معرف التدفق
  contact_id: string;            // معرف جهة الاتصال
  response_data: object;         // البيانات المجمعة (JSON)
  submitted_at: string;          // تاريخ الإرسال
  message_id?: string;           // معرف الرسالة المرتبطة
  project_id: string;            // معرف المشروع
}

// ملاحظة: غير ظاهر في الصور - تم الاستنتاج من تبويب "الردود"
```

### 3.7 WhatsAppNumber

```typescript
interface WhatsAppNumber {
  id: string;                    // المعرف الفريد
  phone_number: string;          // رقم الهاتف (+201...)
  display_name: string;          // الاسم المعروض (محمد عزاب)
  type: 'production' | 'sandbox'; // النوع
  status: 'active' | 'inactive' | 'pending'; // الحالة
  whatsapp_business_account_id: string; // WABA ID
  phone_number_id: string;       // Phone Number ID من Meta
  access_token_encrypted?: string; // Access Token (مشفر)
  webhook_verify_token?: string; // Webhook Verify Token
  is_default: boolean;           // هل هو الرقم الافتراضي
  health_status?: 'healthy' | 'unhealthy' | 'unknown'; // حالة الصحة
  last_health_check_at?: string; // آخر فحص صحة
  created_at: string;            // تاريخ الإنشاء
  project_id: string;            // معرف المشروع
}

// مثال من الشاشة F:
{
  "id": "num-1",
  "phone_number": "201099884670",
  "display_name": "محمد عزاب",
  "type": "production",
  "status": "active",
  "whatsapp_business_account_id": "873908352481274",
  "is_default": true,
  "created_at": "2025-12-01T00:00:00Z"
}
```

### 3.8 SandboxSession

```typescript
interface SandboxSession {
  id: string;                    // المعرف الفريد
  test_phone_number: string;     // رقم الاختبار (201092750351)
  activation_code: string;       // رمز التفعيل (MPTYQU)
  status: 'pending_activation' | 'active' | 'expired'; // الحالة
  is_activated: boolean;         // هل تم التفعيل
  sandbox_number: string;        // رقم صندوق الرمل (+56920403095)
  activated_at?: string;         // تاريخ التفعيل
  expires_at?: string;           // تاريخ الانتهاء
  created_at: string;            // تاريخ الإنشاء
  project_id: string;            // معرف المشروع
}

// مثال من الشاشة G:
{
  "id": "sandbox-1",
  "test_phone_number": "201092750351",
  "activation_code": "MPTYQU",
  "status": "pending_activation",
  "is_activated": false,
  "sandbox_number": "+56920403095",
  "created_at": "2026-01-07T00:00:00Z"  // "منذ حوالي شهر"
}
```

### 3.9 MediaFile

```typescript
interface MediaFile {
  id: string;                    // المعرف الفريد
  file_name: string;             // اسم الملف (image_cd37814e.jpeg)
  file_type: 'image' | 'video' | 'document' | 'audio'; // النوع
  mime_type: string;             // نوع MIME (image/jpeg, video/mp4)
  file_size: number;             // الحجم بالبايت
  file_url: string;              // رابط التخزين (Blob)
  thumbnail_url?: string;        // رابط الصورة المصغرة
  whatsapp_media_id?: string;    // معرف الوسائط في واتساب
  message_id?: string;           // معرف الرسالة المرتبطة
  contact_id?: string;           // معرف جهة الاتصال
  uploaded_at: string;           // تاريخ الرفع
  status: 'uploaded' | 'failed'; // الحالة
  project_id: string;            // معرف المشروع
}

// مثال من الشاشة E/H:
{
  "id": "media-1",
  "file_name": "image_cd37814e.jpeg",
  "file_type": "image",
  "mime_type": "image/jpeg",
  "file_size": 95334,  // 93.1 KB
  "file_url": "https://blob.vercel-storage.com/...",
  "status": "uploaded",
  "uploaded_at": "2026-02-06T23:28:00Z"  // "6 فبراير، 11:28 م"
}
```

### 3.10 DashboardStats

```typescript
interface DashboardStats {
  date_range: {
    start: string;               // تاريخ البداية (ISO 8601)
    end: string;                 // تاريخ النهاية
  };
  workflows: {
    total_active: number;        // عدد سير العمل النشطة
    total_executions: number;    // إجمالي التنفيذات
  };
  logs: {
    total_errors: number;        // عدد الأخطاء (71)
    total_logs: number;          // إجمالي السجلات (490)
  };
  messages: {
    total: number;               // إجمالي الرسائل (235)
    inbound: number;             // الواردة (71)
    outbound: number;            // الصادرة (164)
  };
  numbers: {
    total: number;               // عدد الأرقام (1)
    active: number;              // الأرقام النشطة (1)
  };
  daily_messages: DailyMessageCount[]; // الرسائل اليومية
}

interface DailyMessageCount {
  date: string;                  // التاريخ (2026-02-03)
  inbound: number;               // الرسائل الواردة
  outbound: number;              // الرسائل الصادرة
}

// مثال من الشاشة I:
{
  "date_range": {
    "start": "2026-02-01",
    "end": "2026-02-07"
  },
  "workflows": {
    "total_active": 0,
    "total_executions": 0
  },
  "logs": {
    "total_errors": 71,
    "total_logs": 490
  },
  "messages": {
    "total": 235,
    "inbound": 71,
    "outbound": 164
  },
  "numbers": {
    "total": 1,
    "active": 1
  },
  "daily_messages": [
    { "date": "2026-02-03", "inbound": 80, "outbound": 20 },
    { "date": "2026-02-04", "inbound": 40, "outbound": 10 }
  ]
}
```

---

## (4) قائمة APIs المطلوبة (Endpoints)

### 4.1 Conversations & Messages API

```
GET    /api/conversations
  Query: status, assigned_to, search, page, limit
  Response: { conversations: Conversation[], total: number, page: number }

GET    /api/conversations/:id
  Response: { conversation: Conversation, messages: WhatsAppMessage[] }

POST   /api/conversations/:id/messages
  Body: { content?, media_file?, type }
  Response: { message: WhatsAppMessage }

PATCH  /api/conversations/:id
  Body: { status?, assigned_to?, tags? }
  Response: { conversation: Conversation }

POST   /api/conversations/:id/notes
  Body: { note: string }
  Response: { note: Note }
```

**ربط بالشاشة A:**
- عرض قائمة المحادثات (3 محادثات ظاهرة)
- عرض رسائل محادثة محددة (يد محمد154)
- إرسال رسالة جديدة (نص أو صورة)
- تحديث حالة المحادثة
- إضافة ملاحظة

---

### 4.2 Templates API

```
GET    /api/templates
  Query: whatsapp_number_id, status, category, language, search
  Response: { templates: WhatsAppTemplate[], total: number }

POST   /api/templates
  Body: { name, category, language, components }
  Response: { template: WhatsAppTemplate }

GET    /api/templates/:id
  Response: { template: WhatsAppTemplate }

PATCH  /api/templates/:id
  Body: { components?, status? }
  Response: { template: WhatsAppTemplate }

DELETE /api/templates/:id
  Response: { success: boolean }

POST   /api/templates/sync
  Body: { whatsapp_number_id }
  Response: { synced: number, templates: WhatsAppTemplate[] }

POST   /api/templates/:id/send
  Body: { contact_id, variables }
  Response: { message: WhatsAppMessage }
```

**ربط بالشاشة B:**
- عرض 4 قوالب (technician_assigned, booking_confirmation, order_created, test_service)
- إنشاء قالب جديد
- مزامنة من Meta
- إرسال قالب لجهة اتصال

---

### 4.3 Flows API

```
GET    /api/flows
  Query: whatsapp_number_id, status
  Response: { flows: WhatsAppFlow[], total: number }

POST   /api/flows
  Body: { name, json_schema }
  Response: { flow: WhatsAppFlow }

GET    /api/flows/:id
  Response: { flow: WhatsAppFlow }

PATCH  /api/flows/:id
  Body: { name?, json_schema?, status? }
  Response: { flow: WhatsAppFlow }

DELETE /api/flows/:id
  Response: { success: boolean }

POST   /api/flows/sync
  Body: { whatsapp_number_id }
  Response: { synced: number, flows: WhatsAppFlow[] }

GET    /api/flows/:id/responses
  Query: page, limit
  Response: { responses: FlowResponse[], total: number }
```

**ربط بالشاشة C:**
- عرض التدفقات (حالة فارغة حالياً)
- إنشاء تدفق جديد
- مزامنة من Meta
- عرض الردود في تبويب منفصل

---

### 4.4 Contacts API

```
GET    /api/contacts
  Query: search, page, limit
  Response: { contacts: WhatsAppContact[], total: number }

GET    /api/contacts/:id
  Response: { contact: WhatsAppContact, conversations: Conversation[] }

POST   /api/contacts
  Body: { phone_number, display_name }
  Response: { contact: WhatsAppContact }

PATCH  /api/contacts/:id
  Body: { display_name?, tags?, notes? }
  Response: { contact: WhatsAppContact }

DELETE /api/contacts/:id
  Response: { success: boolean }
```

**ربط بالشاشة D:**
- عرض 112 جهة اتصال (20 في الصفحة)
- البحث بالرقم أو الاسم
- عرض تفاصيل جهة اتصال
- Pagination (6 صفحات)

---

### 4.5 Media API

```
GET    /api/media
  Query: type, date_range, page, limit
  Response: { media: MediaFile[], total: number }

POST   /api/media/upload
  Body: FormData (file)
  Response: { media: MediaFile }

GET    /api/media/:id
  Response: { media: MediaFile, download_url: string }

DELETE /api/media/:id
  Response: { success: boolean }
```

**ربط بالشاشة E/H:**
- عرض 1716 ملف (50 في الصفحة)
- فلترة بالنوع والتاريخ
- رفع ملف جديد
- تنزيل أو حذف ملف

---

### 4.6 WhatsApp Numbers API

```
GET    /api/whatsapp-numbers
  Query: type, status
  Response: { numbers: WhatsAppNumber[], total: number }

POST   /api/whatsapp-numbers
  Body: { phone_number, display_name, type, phone_number_id, access_token }
  Response: { number: WhatsAppNumber }

GET    /api/whatsapp-numbers/:id
  Response: { number: WhatsAppNumber }

PATCH  /api/whatsapp-numbers/:id
  Body: { display_name?, status? }
  Response: { number: WhatsAppNumber }

DELETE /api/whatsapp-numbers/:id
  Response: { success: boolean }

POST   /api/whatsapp-numbers/:id/health-check
  Response: { health_status: string, last_health_check_at: string }
```

**ربط بالشاشة F:**
- عرض رقمين (إنتاج + صندوق رمل)
- إضافة رقم جديد
- فحص صحة الرقم
- تعديل أو حذف رقم

---

### 4.7 Sandbox API

```
GET    /api/sandbox
  Response: { sandbox_number: string, sessions: SandboxSession[] }

POST   /api/sandbox/sessions
  Body: { test_phone_number }
  Response: { session: SandboxSession, activation_instructions: string }

DELETE /api/sandbox/sessions/:id
  Response: { success: boolean }

POST   /api/sandbox/sessions/:id/activate
  Body: { activation_code }
  Response: { session: SandboxSession }
```

**ربط بالشاشة G:**
- عرض رقم صندوق الرمل (+56920403095)
- عرض الجلسات (1 جلسة: 201092750351)
- بدء جلسة اختبار جديدة
- حذف جلسة

---

### 4.8 Dashboard API

```
GET    /api/dashboard/stats
  Query: start_date, end_date
  Response: DashboardStats

GET    /api/dashboard/recent-messages
  Query: limit
  Response: { messages: WhatsAppMessage[] }
```

**ربط بالشاشة I:**
- عرض إحصائيات (0 سير عمل، 71 خطأ، 235 رسالة، 1 رقم)
- عرض الرسائل اليومية (رسم بياني 7 أيام)
- عرض آخر 20 رسالة

---

## (5) Backlog تطوير (مرتب حسب الأولوية)

### P0: منع التشغيل أو فقدان البيانات

1. **[Inbox] تطبيق Real-time Updates للرسائل**
   - **الوصف:** المحادثات والرسائل حالياً ثابتة. يجب تطبيق WebSocket أو Polling لتحديث الرسائل تلقائياً.
   - **المرجع:** الشاشة A - لا توجد آلية تحديث تلقائي ظاهرة
   - **التأثير:** المستخدمون لا يرون الرسائل الجديدة إلا بعد التحديث اليدوي

2. **[Templates] معالجة حالة "تحت الموافقة"**
   - **الوصف:** جميع القوالب (4) في حالة "تحت الموافقة" ولا توجد آلية لتتبع الموافقة من Meta.
   - **المرجع:** الشاشة B - جميع القوالب بشارة خضراء "تحت الموافقة"
   - **التأثير:** لا يمكن استخدام القوالب قبل الموافقة

3. **[Webhook] التحقق من Webhook Signature**
   - **الوصف:** يجب التحقق من توقيع Meta في `/api/webhook` لمنع الرسائل المزيفة.
   - **المرجع:** ملف `app/api/webhook/route.ts` (من المراجعة السابقة)
   - **التأثير:** ثغرة أمنية خطيرة

4. **[Media] حماية روابط Blob**
   - **الوصف:** روابط الملفات في الشاشة E عامة. يجب تطبيق توقيع مؤقت (Signed URLs).
   - **المرجع:** الشاشة E/H - روابط الملفات ظاهرة بدون حماية
   - **التأثير:** يمكن الوصول للملفات بدون صلاحيات

5. **[Numbers] تطبيق Health Check للأرقام**
   - **الوصف:** زر "فحص الصحة" موجود لكن غير متصل بـ API.
   - **المرجع:** الشاشة F - زر "فحص الصحة" غير وظيفي
   - **التأثير:** لا يمكن اكتشاف انقطاع الأرقام

---

### P1: تحسين التشغيل والتحكم

6. **[Inbox] إضافة فلترة متقدمة**
   - **الوصف:** الفلترة محدودة بـ "الحالة" و"الموكل". يجب إضافة: التاريخ، الوسوم، نوع آخر رسالة.
   - **المرجع:** الشاشة A - Dropdowns محدودة
   - **التأثير:** صعوبة في العثور على محادثات قديمة

7. **[Inbox] تطبيق Assignment للموظفين**
   - **الوصف:** قسم "التعيين" يظهر "لم يتم تعريفيه" لكن لا توجد آلية للتعيين.
   - **المرجع:** الشاشة A (الجانب الأيمن) - "لم يتم تعريفيه"
   - **التأثير:** لا يمكن توزيع المحادثات على الفريق

8. **[Templates] دعم المعاينة التفاعلية**
   - **الوصف:** المعاينة نصية فقط. يجب عرض القالب كما سيظهر في واتساب (مع الأزرار والصور).
   - **المرجع:** الشاشة B - عمود "معاينة" نصي فقط
   - **التأثير:** صعوبة في تقييم تصميم القالب

9. **[Templates] إضافة محرر مرئي**
   - **الوصف:** زر "إنشاء تدفق +" يحتاج لنموذج إنشاء قالب مع محرر WYSIWYG.
   - **المرجع:** الشاشة B - زر بدون نموذج
   - **التأثير:** لا يمكن إنشاء قوالب من داخل النظام

10. **[Flows] بناء محرر التدفقات**
    - **الوصف:** الشاشة فارغة تماماً. يحتاج لمحرر مرئي (Drag & Drop) لبناء Forms.
    - **المرجع:** الشاشة C - حالة فارغة "لا توجد بيانات متاحة"
    - **التأثير:** لا يمكن استخدام ميزة Flows

11. **[Contacts] إضافة Import/Export**
    - **الوصف:** 112 جهة اتصال موجودة لكن لا توجد طريقة لاستيراد أو تصدير CSV.
    - **المرجع:** الشاشة D - لا توجد أزرار استيراد/تصدير
    - **التأثير:** صعوبة في إدارة جهات الاتصال بالجملة

12. **[Contacts] إضافة نظام الوسوم (Tags)**
    - **الوصف:** لا يوجد عمود "الوسوم" في الجدول ولا في تفاصيل جهة الاتصال.
    - **المرجع:** الشاشة D - الأعمدة محدودة (معرف، اسم، تأسيس)
    - **التأثير:** لا يمكن تصنيف العملاء

13. **[Media] إضافة معاينة الملفات**
    - **الوصف:** أيقونة "عرض (👁)" موجودة لكن غير وظيفية.
    - **المرجع:** الشاشة E/H - عمود "الإجراءات" مع أيقونات
    - **التأثير:** يجب تنزيل الملف لمعاينته

14. **[Numbers] تطبيق حد الأرقام (Rate Limiting)**
    - **الوصف:** التنبيه يقول "وصلت إلى حد 1 رقم" لكن لا توجد آلية للترقية.
    - **المرجع:** الشاشة F - تنبيه معلوماتي + زر "الترقية"
    - **التأثير:** لا يمكن إضافة أرقام إضافية

15. **[Sandbox] تطبيق التفعيل التلقائي**
    - **الوصف:** رمز التفعيل "MPTYQU" يدوي. يجب إرسال رسالة واتساب تلقائياً للتفعيل.
    - **المرجع:** الشاشة G - حالة "قيد التفعيل" + "ليس بعد"
    - **التأثير:** خطوات يدوية إضافية للاختبار

16. **[Dashboard] تطبيق تصدير التقارير**
    - **الوصف:** الرسم البياني والإحصائيات موجودة لكن لا يوجد زر تصدير PDF/CSV.
    - **المرجع:** الشاشة I - لا توجد أزرار تصدير
    - **التأثير:** لا يمكن مشاركة التقارير خارج النظام

---

### P2: تحسين تجربة المستخدم

17. **[Inbox] إضافة ردود سريعة**
    - **الوصف:** لا يوجد زر "ردود جاهزة" في منطقة الرد.
    - **المرجع:** الشاشة A - حقل "اكتب رسالتك..." بدون ميزات إضافية
    - **التأثير:** الموظفون يكتبون نفس الردود يدوياً

18. **[Inbox] إضافة إشعارات صوتية**
    - **الوصف:** لا توجد إشعارات مرئية أو صوتية للرسائل الجديدة.
    - **المرجع:** الشاشة A - لا توجد آلية تنبيه ظاهرة
    - **التأثير:** الموظفون يفوّتون رسائل عاجلة

19. **[Inbox] تحسين عرض الصور**
    - **الوصف:** الصور (3) ظاهرة لكن بدون معرض صور (Gallery) للتكبير والتنقل.
    - **المرجع:** الشاشة A - الصور مصغرة بدون lightbox
    - **التأثير:** صعوبة في استعراض التفاصيل

20. **[Templates] إضافة إحصائيات الأداء**
    - **الوصف:** لا توجد معلومات عن عدد مرات الإرسال أو معدل الفتح لكل قالب.
    - **المرجع:** الشاشة B - الجدول لا يحتوي عمود "الإحصائيات"
    - **التأثير:** لا يمكن قياس فعالية القوالب

21. **[Templates] دعم لغات متعددة**
    - **الوصف:** جميع القوالب (4) بالعربية فقط. يجب دعم قوالب متعددة اللغات لنفس القالب.
    - **المرجع:** الشاشة B - عمود "اللغة" يظهر "AR" فقط
    - **التأثير:** لا يمكن خدمة عملاء بلغات أخرى

22. **[Flows] إضافة معاينة قبل النشر**
    - **الوصف:** لا توجد ميزة "معاينة" للتدفق قبل نشره على واتساب.
    - **المرجع:** الشاشة C - حالة فارغة (لا توجد عناصر)
    - **التأثير:** أخطاء في التدفقات لا تُكتشف قبل النشر

23. **[Contacts] إضافة سجل الأنشطة**
    - **الوصف:** تفاصيل جهة الاتصال محدودة. يجب إضافة Timeline بجميع الأنشطة (رسائل، ملاحظات، تغييرات).
    - **المرجع:** الشاشة D - عرض محدود للمعلومات
    - **التأثير:** لا يمكن تتبع تاريخ العلاقة مع العميل

24. **[Media] إضافة فلترة حسب جهة الاتصال**
    - **الوصف:** الفلترة بالتاريخ فقط. يجب إضافة فلترة بـ "جهة الاتصال" أو "الرقم".
    - **المرجع:** الشاشة E/H - التبويبات محدودة (اليوم، الأسبوع، الشهر، الكل)
    - **التأثير:** صعوبة في العثور على ملفات عميل معين

25. **[Media] تطبيق Thumbnails للفيديو**
    - **الوصف:** الفيديوهات تظهر بنفس أيقونة الملفات. يجب عرض صورة مصغرة.
    - **المرجع:** الشاشة E/H - عمود "النوع" يظهر "الفيديو" لكن بدون معاينة
    - **التأثير:** صعوبة في التعرف على محتوى الفيديو

26. **[Numbers] إضافة سجل الصحة (Health Logs)**
    - **الوصف:** "آخر فحص صحة" غير ظاهر. يجب عرض سجل تاريخي بحالات الصحة.
    - **المرجع:** الشاشة F - لا يوجد عرض لسجل الفحوصات
    - **التأثير:** لا يمكن اكتشاف الأعطال المتكررة

27. **[Sandbox] إضافة تعليمات التفعيل**
    - **الوصف:** الرمز "MPTYQU" ظاهر لكن لا توجد تعليمات واضحة كيفية استخدامه.
    - **المرجع:** الشاشة G - رمز التفعيل مع رابط "مفتوح" غير واضح
    - **التأثير:** المستخدمون الجدد لا يعرفون ماذا يفعلون

28. **[Dashboard] تحسين الرسم البياني**
    - **الوصف:** الرسم البياني بسيط. يجب إضافة: Tooltip، Zoom، مقارنة فترات سابقة.
    - **المرجع:** الشاشة I - رسم بياني أعمدة بسيط
    - **التأثير:** تحليل محدود للبيانات

29. **[Dashboard] إضافة ويدجت "Quick Actions"**
    - **الوصف:** بطاقات الإجراءات السريعة (4) موجودة لكن غير وظيفية.
    - **المرجع:** الشاشة I - بطاقات "أضف رقم الهاتف"، "إنشاء webhook"، إلخ
    - **التأثير:** مجرد عناصر زينة بدون فائدة

30. **[Dashboard] تحسين قسم "الرسائل الحديثة"**
    - **الوصف:** الجدول يعرض 20 رسالة لكن بدون روابط مباشرة للمحادثات.
    - **المرجع:** الشاشة I - جدول الرسائل الحديثة
    - **التأثير:** يجب البحث يدوياً عن المحادثة

---

## الخلاصة

### الإنجازات الحالية:
- بنية تحتية قوية (Next.js 16 + Supabase + TypeScript)
- 9 شاشات رئيسية مبنية بواجهة عربية متقنة
- قاعدة بيانات شاملة (23 جدول)
- تكاملات جاهزة (Supabase, Redis, Blob, AI Gateway)
- نظام Webhook + Queue

### الفجوات الرئيسية:
1. **Flows فارغة تماماً** - يحتاج لبناء كامل
2. **Templates قيد الموافقة** - لا توجد آلية موافقة
3. **Real-time غير موجود** - المحادثات ثابتة
4. **معظم الأزرار غير وظيفية** - تحتاج ربط بـ APIs

### الأولويات:
1. P0: أمان Webhook + Real-time Updates + Health Checks (5 مهام)
2. P1: إكمال الميزات الأساسية (Templates Editor, Flows Builder, Assignment) (11 مهمة)
3. P2: تحسينات UX (Notifications, Gallery, Analytics) (14 مهمة)

**إجمالي المهام المطلوبة: 30 مهمة**

---

*تم إنشاء هذه الوثيقة بناءً على التحليل المرئي الكامل للشاشات المرفقة دون أي افتراضات خارجية.*
